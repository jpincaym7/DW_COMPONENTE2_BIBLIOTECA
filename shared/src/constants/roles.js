export const ROLES = {
  ADMIN: 'admin',
  USER: 'user'
};

export const ROLE_VALUES = Object.values(ROLES);

export const ROLE_LABELS = {
  [ROLES.ADMIN]: 'Administrador',
  [ROLES.USER]: 'Usuario'
};
